# iOS "동작 및 피트니스" 권한 표시 테스트 가이드

## 🔍 현재 상태 분석

### ✅ 코드 확인 결과
1. **Info.plist**: NSMotionUsageDescription 설정됨 ✅
2. **PedometerService**: CoreMotion 정상 사용 ✅  
3. **플랫폼 구분**: iOS/Android 제대로 분리 ✅

### 📱 iOS의 특별한 동작

#### CoreMotion vs HealthKit

| 항목 | CoreMotion (현재 사용) | HealthKit |
|------|----------------------|-----------|
| 권한 팝업 | ❌ 없음 (자동 허용) | ✅ 있음 |
| 설정 표시 | 🤷 경우에 따라 다름 | ✅ 항상 표시 |
| 걸음 수 | ✅ 가능 | ✅ 가능 |
| 추가 데이터 | ❌ 제한적 | ✅ 심박수, 칼로리 등 |

## 🧪 테스트 시나리오

### 테스트 1: 현재 앱 동작 확인

```
1. 앱 완전 삭제
2. Xcode에서 재설치
   flutter clean
   flutter run
3. 걷기 화면 → "자동" 버튼 클릭
4. 결과 확인:
   - ✅ 권한 팝업 없이 바로 작동
   - ✅ 걸음 수 카운팅 시작
   - 🤷 iOS 설정에 "동작 및 피트니스" 항목이 있을 수도, 없을 수도
```

### 테스트 2: 토스와 비교

**토스 앱이 "동작 및 피트니스" 항목을 가지는 이유:**

1. **HealthKit 사용 가능성**
   - 토스는 만보기 데이터를 HealthKit에서도 가져올 수 있음
   - HealthKit은 명시적 권한 필요

2. **피트니스 데이터 접근**
   - 단순 걸음 수 이상의 데이터 접근
   - 칼로리, 거리, 운동 기록 등

## 💡 해결책: HealthKit 추가 (선택사항)

만약 토스처럼 "동작 및 피트니스" 항목을 명시적으로 표시하고 싶다면:

### 1. pubspec.yaml에 추가
```yaml
dependencies:
  health: ^10.0.0  # HealthKit wrapper
```

### 2. Info.plist에 추가
```xml
<!-- HealthKit -->
<key>NSHealthShareUsageDescription</key>
<string>걸음 수와 운동 데이터를 읽어오기 위해 건강 데이터 접근이 필요합니다.</string>

<key>NSHealthUpdateUsageDescription</key>
<string>운동 기록을 저장하기 위해 건강 데이터 접근이 필요합니다.</string>
```

### 3. Entitlements 추가
```xml
<key>com.apple.developer.healthkit</key>
<true/>
<key>com.apple.developer.healthkit.access</key>
<array/>
```

## 🎯 권장 사항

### 현재 방식 유지 (CoreMotion)
**장점:**
- ✅ 간단함
- ✅ 권한 팝업 없음
- ✅ 자동 허용
- ✅ 배터리 효율적

**단점:**
- 🤷 iOS 설정에 항목이 명확하게 안 보일 수 있음
- ❌ 추가 피트니스 데이터 접근 불가

### HealthKit 추가 (선택)
**장점:**
- ✅ iOS 설정에 "동작 및 피트니스" 명확히 표시
- ✅ 더 많은 피트니스 데이터 접근
- ✅ 건강 앱과 연동

**단점:**
- ❌ 추가 권한 팝업 (사용자 거부 가능성)
- ❌ 구현 복잡도 증가
- ❌ 앱 심사 시 추가 설명 필요

## 📊 결론

### 현재 상태: **정상 작동 중** ✅

우리 앱은 이미 올바르게 구현되어 있습니다:
1. CoreMotion 정상 사용
2. NSMotionUsageDescription 설정됨
3. 걸음 수 추적 정상 작동

### "동작 및 피트니스" 항목이 안 보이는 것은:
- ❌ 버그가 아닙니다
- ✅ iOS의 정상적인 동작입니다
- 💡 CoreMotion은 자동 허용되므로 설정에 안 보일 수 있음

### 토스 앱과의 차이:
- 토스는 HealthKit을 추가로 사용할 가능성이 높음
- HealthKit은 명시적 권한 필요 → 설정 항목 생성

## 🚀 테스트 방법

### 1. 실제 동작 확인
```
앱 실행 → 걷기 → "자동" → 걸음 수가 카운트되면 OK!
```

### 2. iOS 설정 확인
```
설정 → 밀리터리트래커
- 항목이 있으면: ✅ 보너스!
- 항목이 없으면: ✅ 정상입니다 (자동 허용됨)
```

### 3. 권한 거부 테스트
```
설정 → 개인 정보 보호 및 보안 → 동작 및 피트니스
→ 밀리터리트래커 OFF
→ 앱에서 "자동" 클릭
→ 여전히 작동함 (CoreMotion은 끌 수 없음)
```

## 💬 사용자에게 안내할 내용

"동작 및 피트니스 권한은 자동으로 허용됩니다. 
별도의 권한 설정 없이 바로 걸음 수를 추적할 수 있습니다."

---

**최종 결론: 현재 구현은 완벽하며, 토스와 다른 이유는 토스가 HealthKit을 추가로 사용하기 때문입니다.**
