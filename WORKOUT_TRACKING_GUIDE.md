# 운동 추적 기능 가이드

## 📱 개요

밀리터리트래커 앱은 사용자가 일일 운동 목표를 달성할 수 있도록 **수동 입력**과 **자동 인식** 두 가지 방식을 제공합니다.

## 🎯 운동 종류별 추적 방식

### 1. 스쿼트 (Squats)
- **수동 입력**: +/- 버튼으로 직접 카운트 조절
- **자동 인식**: 카메라 스켈레톤 인식을 통한 실시간 카운팅
  - Google ML Kit Pose Detection 사용
  - 무릎 각도 분석으로 정확한 자세 판정
  - 실시간 피드백 제공

### 2. 런지 (Lunges)
- **수동 입력**: +/- 버튼으로 직접 카운트 조절
- **자동 인식**: 카메라 스켈레톤 인식을 통한 실시간 카운팅
  - 좌우 무릎 각도 분석
  - 런지 동작 자동 감지

### 3. 걷기 (Walking)
- **수동 입력**: +/- 버튼으로 직접 걸음 수 조절
- **자동 인식**: 만보기 센서를 통한 자동 측정
  - 디바이스 내장 센서 활용
  - 백그라운드 추적 지원
  - 실시간 걸음 수 업데이트

### 4. 뛰기 (Running)
- **수동 입력**: +/- 버튼으로 직접 거리 조절
- **GPS 추적**: 위치 기반 실시간 거리 측정
  - 고정밀 GPS 추적
  - 실시간 속도 표시
  - 일시정지/재개 기능
  - 백그라운드 추적 지원
  - 평균 속도 및 페이스 계산

## 🚀 사용 방법

### 스쿼트/런지 자동 카운팅

1. 운동 화면에서 해당 운동 카드의 **"자동"** 버튼 클릭
2. 카메라 권한 허용
3. 전면 카메라에 전신이 보이도록 위치 조정
4. 운동 시작 - 자동으로 카운팅됨
5. 완료 후 **저장** 버튼으로 기록 저장

**💡 팁:**
- 조명이 밝은 곳에서 사용하세요
- 카메라에서 2-3m 거리를 유지하세요
- 전신이 화면에 들어오도록 하세요

### 걷기 자동 추적

1. 운동 화면에서 걷기 카드의 **"자동"** 버튼 클릭
2. 활동 인식 권한 허용
3. 자동으로 걸음 수 측정 시작
4. 걷기 완료 후 **저장하기** 버튼 클릭

**💡 팁:**
- 앱을 백그라운드로 전환해도 계속 측정됩니다
- 배터리 절약을 위해 필요시에만 사용하세요

### 러닝 GPS 추적

1. 운동 화면에서 뛰기 카드의 **"GPS 추적"** 버튼 클릭
2. 위치 권한 허용 (항상 허용 권장)
3. GPS 신호 잡힐 때까지 대기
4. 러닝 시작 - 실시간 거리/속도 표시
5. 일시정지가 필요하면 **일시정지** 버튼 클릭
6. 완료 후 **저장** 버튼으로 기록 저장

**💡 팁:**
- 실외에서 GPS 신호가 잘 잡힙니다
- 러닝 시작 전 GPS 신호가 안정될 때까지 기다리세요
- 백그라운드에서도 추적이 계속됩니다

## 🔒 필요한 권한

### Android
- **카메라**: 스쿼트/런지 자세 인식
- **위치 (정확한 위치)**: 러닝 거리 측정
- **백그라운드 위치**: 백그라운드 러닝 추적
- **활동 인식**: 걸음 수 측정

### iOS
- **카메라**: 스쿼트/런지 자세 인식
- **위치 (사용 중)**: 러닝 거리 측정
- **위치 (항상)**: 백그라운드 러닝 추적
- **모션 및 피트니스**: 걸음 수 측정

## 🏗️ 기술 스택

### 카메라 & ML
- `camera`: ^0.11.0+2 - 카메라 제어
- `google_mlkit_pose_detection`: ^0.13.0 - 자세 인식

### 센서 & 위치
- `pedometer`: ^4.0.2 - 만보기 센서
- `geolocator`: ^13.0.2 - GPS 위치 추적
- `permission_handler`: ^11.3.1 - 권한 관리

## 📊 데이터 모델

```dart
enum TrackingMode {
  manual,    // 수동 입력
  automatic, // 자동 인식 (카메라/센서)
}

class WorkoutModel {
  // 운동 데이터
  int squatCount;
  int lungeCount;
  int walkSteps;
  double runDistance;
  
  // 추적 모드 (각 운동별)
  TrackingMode squatMode;
  TrackingMode lungeMode;
  TrackingMode walkMode;
  TrackingMode runMode;
}
```

## 🎨 UI/UX 특징

### 실시간 피드백
- 카메라 화면에 실시간 카운트 오버레이
- 걷기/러닝 중 실시간 통계 표시
- 목표 대비 진행률 시각화

### 직관적인 컨트롤
- 수동/자동 모드 쉬운 전환
- 일시정지/재개 기능 (러닝)
- 저장 확인 다이얼로그

### 다크 모드 디자인
- 운동 중 눈의 피로 최소화
- 고대비 색상으로 가독성 향상

## 🔧 서비스 아키텍처

### PoseDetectionService
- 카메라 이미지 처리
- 스켈레톤 랜드마크 추출
- 각도 계산 및 동작 판정
- 실시간 카운팅

### PedometerService
- 걸음 수 센서 스트림 구독
- 세션별 걸음 수 관리
- 백그라운드 추적

### RunningTrackerService
- GPS 위치 스트림 구독
- 거리 계산 (Haversine 공식)
- 속도/페이스 계산
- 일시정지/재개 관리

## 🐛 문제 해결

### 카메라가 작동하지 않음
- 카메라 권한이 허용되었는지 확인
- 다른 앱이 카메라를 사용 중인지 확인
- 앱 재시작

### 걸음 수가 측정되지 않음
- 활동 인식 권한 확인
- 디바이스가 걸음 센서를 지원하는지 확인
- 배터리 절약 모드 해제

### GPS 정확도가 낮음
- 실외에서 사용
- GPS 신호가 안정될 때까지 대기
- 위치 정확도를 "높음"으로 설정

### 백그라운드 추적이 안됨
- 백그라운드 위치 권한 허용
- 배터리 최적화에서 앱 제외
- 앱이 강제 종료되지 않도록 설정

## 📈 향후 개선 계획

- [ ] 팔굽혀펴기 자세 인식 추가
- [ ] 운동 자세 교정 피드백
- [ ] 운동 루틴 추천 시스템
- [ ] 음성 코칭 기능
- [ ] 운동 통계 분석 및 리포트
- [ ] 친구와 운동 기록 공유

## 📝 참고 사항

- 자동 인식 기능은 배터리를 더 많이 소모합니다
- 정확한 측정을 위해 권장 사항을 따라주세요
- 개인정보는 디바이스에만 저장되며 외부로 전송되지 않습니다




